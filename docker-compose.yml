version: '2'
services:
  app:
    restart: always
    build:
      context: .
      dockerfile: .docker/Dockerfile
    image: docker-server-mp
    ports:
      - 8090:80
    volumes:
      - ./src:/var/www/html
    environment:
